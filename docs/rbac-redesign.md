# RBAC 权限管理重构

## 角色设计

- superuser、website_admin、student_advisor、student、visitor + 自定义角色，全部平等（无系统/自定义区分）
- 每个用户只能有一个角色
- visitor 是已注册用户但权限最低

## 权限设计

- 权限采用树形结构（用户中心/管理后台 → 面板 → 功能），前后端同时控制
- 通过树形复选框给角色分配权限

## 背景

现有 RBAC 权限码扁平，无法直观对应前端页面结构，管理不便。权限树需要覆盖前端路由和后端 API 两个维度。
