# 生产/共享配置
# 同时安装 glibc(宿主机) 和 musl(Alpine容器) 的原生包
supportedArchitectures[libc][]=glibc
supportedArchitectures[libc][]=musl

# react-pdf 需要 pdfjs-dist 提升到根 node_modules
public-hoist-pattern[]=pdfjs-dist
